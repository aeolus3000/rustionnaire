use std::cell::RefCell;
use rustionnaire::utils::questionnaire::Questionnaire;
use rustionnaire::utils::evaluation::Evaluation;
use rustionnaire::utils::questitem::QuestItem;
use rustionnaire::utils::engine::Engine;

fn main() {
    /*
    let engine = Engine{};
    let filename = getFilenameFromStdIn();
    let questionnaire = readQuestionnaire(filename);
    let evaluation = engine.execute_questionnaire(questionnaire);
    engine.show(evaluation);
     */
}